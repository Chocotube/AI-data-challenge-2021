from setuptools import setup, find_packages

setup(name="covidpredictions", version='0.1.0', packages=find_packages(include=['DecisionTree', 'DecisionTree.*', 'LogisticRegression', 'LogisticRegression.*']))
